env:
  BUILDKITE_CLEAN_CHECKOUT: true

steps:
  - label: "e2e load"
    command: 
      -  "cd performance/caliper"
      -  "./prepare.sh"
      -  "cd smartcontract/hello"
      -  "npm install"
      -  "cd ../../networks/staging"
      -  "./prepare_run.sh"
      -  "./setup_account_multinode_miners.sh"
      -  "./run_multinode_miners.sh"
    agents:
      - "host=e2e.mantis.ws"
    artifact_paths: 
      - "performance/caliper/caliper-benchmarks/report.html"
